Ext.define('AffiliatedHospital.model.outpatient.ReserveDoctorTime', {
    extend: 'Ext.data.Model',
    config: {
        fields: [
            '_id',
            'num',
            'time'

        ]
    }
});
